%!

/TM 775 def  %top margin
/BM -12 def   %bottom
/LM 0 def    %left
/RM 612 def  %right


/newline
 {currentpoint 13 sub
  exch pop LM
  exch moveto} def

/nlIfNec
 {currentpoint pop RM gt  %beyond RM?
   {newline}if} def       %yes:next line


/done?    %stack:---bool.
 {currentpoint exch pop   %below BM?
  BM lt} def

/fillpage     %stack:str
 {/strg exch def
  { {pop pop nlIfNec} strg kshow
     done? {exit} if
  }loop
}def

/Times-Bold findfont 10 scalefont setfont
LM TM moveto
.7 setgray
( MIRIAD ) fillpage

/Times-Roman findfont 50 scalefont setfont


RM LM sub      %center the words
(MIRIAD)stringwidth pop sub
2 div 500 moveto
0 setgray
(MIRIAD)show
/Times-Roman findfont 20 scalefont setfont
RM LM sub (Multichannel Image Reconstruction,) stringwidth pop sub
2 div 460 moveto
(Multichannel Image Reconstruction,)show
RM LM sub (Image Analysis, and Display) stringwidth pop sub
2 div 430 moveto
(Image Analysis, and Display)show
/Times-Roman findfont 50 scalefont setfont
RM LM sub (User's Guide) stringwidth pop sub
2 div 360 moveto
(User's Guide)show
/Times-Roman findfont 20 scalefont setfont
RM LM sub (Version 1.0) stringwidth pop sub
2 div 75 moveto
(Version 1.0) show
/Times-Roman findfont 20 scalefont setfont
RM LM sub (March 1991) stringwidth pop sub
2 div 50 moveto
(March 1991) show

showpage
