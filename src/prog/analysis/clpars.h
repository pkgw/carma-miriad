c ----------------------------------------------------------------------
c common include file for clfind, clplot and clstat
c
	integer maxbuf,maxdim
	parameter(maxbuf=512*512*512,maxdim=2048)
c ----------------------------------------------------------------------
